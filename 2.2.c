#include<stdio.h>
#include<conio.h>
void main()
{
	clrscr();
	int i=0;
	int limit=100;
	while(i<lim-1)/*for(i=0;i<lim-1&&(c=getchar())!='\n'&&c!=EOF;i++)*/
	{
		c=getchar();
		if(c==EOF)
		{
			break;
		}
		elseif(c=='\n')
		{
			break;
		}
		i++;
	}
	getch();
}