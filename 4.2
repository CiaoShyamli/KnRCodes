#incllude<math.h>/* adding  pow() function */

float atof( char s[] )
{
  double val,power,exp;
  int i,sign,sign2;
  
  for( i = 0;isspace(s[i]);i++)
  ;
  sign = (s[i]=='-')? -1:1;
 if(s[i] == '+' || s[i] == '-')
    i++;
 for(val = 0.0;isdigit(s[i]);i++)
 {
   val = 10.0*val+(s[i] -'0');
 }
 if(s[i] =='.')
 {
   i++;
    for(power =1.0;isdigit(s[i]);i++)
    {
       val = 10.0 *val+(s[i] - '0');
       pow *= 10.0;
    }
    val *=sign/power;
 }  
  if(s[i]=='e'||s[i]=='E')
  {
    sign2 =  (s[i]=='-')? -1:1;
 if(s[i] == '+' || s[i] == '-')
    i++;
 for(exp = 0.0;isdigit(s[i]);i++)
   exp = 10.0*exp+(s[i] -'0');
   val *= pow(10,sign2 *exp);
  } 
   return val;
    
